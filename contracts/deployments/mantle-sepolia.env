# Mantle Sepolia Testnet Deployment Addresses

# Network Information
NETWORK_NAME="Mantle Sepolia"
CHAIN_ID=5003
RPC_URL="https://mantle-sepolia.drpc.org"
BLOCK_EXPLORER="https://explorer.sepolia.mantle.xyz"

# Deployment Status
DEPLOYMENT_STATUS="PARTIAL - RPC CONNECTIVITY ISSUES"
DEPLOYED_BY="0xE977B29af0a7bc5464B76cEAb68E13E89cA050eA"
DEPLOYMENT_DATE="2024-12-19"

# Successfully Deployed Contracts (VERIFIED ON-CHAIN)
PRICE_ORACLE_ADDRESS="0x1d0E37FAB51be12fC9dDEE4C16663a41B4388CC4"

# Contracts PENDING DEPLOYMENT (RPC timeout issues)
# All remaining contracts need to be deployed when RPC connectivity improves
# or using alternative methods (Remix, different RPC endpoints, etc.)

# Alternative RPC Endpoints to Try:
# - https://rpc.sepolia.mantle.xyz
# - https://5003.rpc.thirdweb.com  
# - https://mantle-sepolia.g.alchemy.com/v2/{api_key}

# Deployment Commands for Remaining Contracts:
# forge script script/DeployDEXRouter.s.sol:DeployDEXRouter --rpc-url {RPC_URL} --private-key {PRIVATE_KEY} --broadcast --legacy
# forge script script/DeployMantleTestnet.s.sol:DeployMantleTestnet --rpc-url {RPC_URL} --private-key {PRIVATE_KEY} --broadcast --legacy